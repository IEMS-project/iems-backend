package com.iems.projectservice.entity.enums;

public enum MemberStatus {
    ACTIVE,      // Member is active and can work on the project
    INACTIVE     // Member is inactive and cannot access the project
}
