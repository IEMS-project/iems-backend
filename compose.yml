services:
  app:
    image: ${IMAGE_NAME}
    container_name: ${SERVICE_NAME}
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${HOST_PORT}:${CONTAINER_PORT}"
    networks:
      - iems-network

networks:
  iems-network:
    external: true
